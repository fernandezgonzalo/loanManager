{
  "name": "loanManager",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ rcn/loanManager",
    "create-local": "graph create --node http://localhost:8020/ rcn/loanManager",
    "remove-local": "graph remove --node http://localhost:8020/ rcn/loanManager",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rcn/loanManager"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.3",
    "@graphprotocol/graph-ts": "0.15.0"
  }
}
